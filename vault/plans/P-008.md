---
id: P-008
status: approved
---
# ゴール
run-queue スキル（`.claude/skills/run-queue/SKILL.md`）に、作成エージェントが着手時にルール（`vault/rules/`）を読む手順を追加する。あわせて `CLAUDE.md` の「## 役割」に、作成エージェントがルールを読む旨を追記する（全体60行以内を維持）。verifier.md・planner.md・フック・install.sh・README.md は変更しない。

これは `docs/roadmap-rules-extension.md` の「## ③ 作成エージェントにルールを渡す（run-queue）」節に対応する。①（P-006/T-0006）②（P-007/T-0007）は完了済みで、`vault/rules/{common,creator,verifier,planner}/`・`docs/vault-spec.md` の「12. ルール」節・`scripts/rules.sh` は既に存在する。

## 分割方針
成果物は「SKILL.md への手順追加」「CLAUDE.md への1行追記」の2点のみで、互いに強く結びついた1つの変更（同一ゴール文からそのまま作れる粒度）のため、1タスクに分割する。

## タスク一覧（順序・依存・各タスクの受け入れ基準の要約）
| 順序 | id | 依存(after) | title | 受け入れ基準の要約 |
|---|---|---|---|---|
| 1 | T-0008 | - | run-queue にルール読み込み手順を追加する | SKILL.md の「## 2. 作る」手順1直後に `rules.sh creator` 読み込みを追加／「## 注意」に vault/rules/ 非編集を追記／進捗記録手順を追加／CLAUDE.md の役割行に追記し60行以内／smoke.sh が fail=0 |

## 計画の受け入れ基準
- 各タスクに成果物と受け入れ基準が1つずつある
- 依存に循環がない
- 1タスクが1コンテキストで終わる粒度である
