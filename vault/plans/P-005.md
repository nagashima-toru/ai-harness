---
id: P-005
status: done
---
# ゴール
todo.md の整合性チェックをフックで行い、壊れた状態のまま先に進めないようにする。新しい PostToolUse フック `.claude/hooks/todo_guard.py` を追加し、以下を壊れとして検出して block する：(a) doing 行が2件以上、(b) blocked なのに question 空、(c) status が5値以外、(d) 「## タスク」表の id 重複、(e) データ行の列数が6でない。todo.md が無い、またはデータ行が無い場合は何もせず許可する。

## 分割方針
フック本体（`todo_guard.py`）・`settings.json` への登録・`scripts/smoke.sh` のケース追加・`docs/vault-spec.md` と `CLAUDE.md` への追記は、1つの整合した変更セットとして検証される（フックだけ足してもテストが無ければ壊れているか確認できず、ドキュメントに書かなければ人が存在に気付けないため）。粒度の基準（受け入れ基準3〜7行・成果物1つ・1コンテキストで完結）を満たすため、分割せず1タスクとする。

## タスク一覧（順序・依存・各タスクの受け入れ基準の要約）
| 順序 | id | after | title | 受け入れ基準の要約 |
|---|---|---|---|---|
| 1 | T-0005 | - | todo_guard フックで todo.md の整合性を PostToolUse でチェックする | (a)〜(e) それぞれで block+reason／todo.md無し・データ行無しで許可／settings.json に PostToolUse 登録／smoke.sh 全件成功／vault-spec.md と CLAUDE.md（60行以内）に追記 |

## 計画の受け入れ基準
- 各タスクに成果物と受け入れ基準が1つずつある
- 依存に循環がない
- 1タスクが1コンテキストで終わる粒度である
