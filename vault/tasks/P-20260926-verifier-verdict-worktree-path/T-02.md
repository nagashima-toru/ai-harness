# T-02 verifier ルール文書への同趣旨の追記を提案ファイルとして下書きする

## 目的
`vault/rules/verifier/verifier.md`（`vault/rules/` 配下、エージェントは直接書き込めない）にも、T-01 と同趣旨の「worktree に入っていても `vault/verdicts/` への書き込み先は必ずメインリポジトリ側のパスにする」旨を反映したい。`docs/vault-spec.md` 12節「提案ファイル方式」に従い、実体ではなく提案ファイルへの下書きを作る。

## 入力
- `vault/rules/verifier/verifier.md`（変更したい対象。読むだけで書き込まない）
- `docs/vault-spec.md` 12節「提案ファイル方式（ルール自体を変更するタスク用）」
- T-01 の成果物（`.claude/agents/verifier.md`。文言を揃えるための参考。T-01 と並行 doing になりうるため、無ければ現状の `.claude/agents/verifier.md` の記述を参考にしてよい）

## 成果物
- `vault/tasks/P-20260926-verifier-verdict-worktree-path/T-02-proposal.md`（1ファイル。`vault/rules/verifier/verifier.md` への追記内容の下書き）

## 受け入れ基準
- `grep -n "vault/rules/verifier/verifier.md" vault/tasks/P-20260926-verifier-verdict-worktree-path/T-02-proposal.md` の出力が1行以上ある（対象ルールファイルのパスを明記している）
- `grep -nE "EnterWorktree|ExitWorktree" vault/tasks/P-20260926-verifier-verdict-worktree-path/T-02-proposal.md` の出力が1行以上ある
- `grep -n "メインリポジトリ" vault/tasks/P-20260926-verifier-verdict-worktree-path/T-02-proposal.md` の出力が1行以上ある
- `grep -n "書き込むのは" vault/tasks/P-20260926-verifier-verdict-worktree-path/T-02-proposal.md` の出力が1行以上ある（既存ルールの該当箇条書きへの追記であることが分かる）
- `git status --porcelain -- . ':!vault/tasks/P-20260926-verifier-verdict-worktree-path/T-02-proposal.md' ':!vault/tasks/P-20260926-verifier-verdict-worktree-path/T-02.md'` の出力が空（提案ファイルとタスク票以外に変更が無い。`vault/rules/` 配下は編集していない）

## 決定済み
- 提案ファイルは全文の書き直しではなく、差分でよい：追記したい1〜3行程度の文面と、挿入位置（`vault/rules/verifier/verifier.md` の「責務」箇条書きにある「書き込むのは `vault/verdicts/<id>.json` だけ。」の行の直後）を書けば十分
- 提案する追記文面には、T-01 と同じ2点（worktree に入っている場合は `EnterWorktree` の後 `ExitWorktree` してからメインリポジトリ側のパスに書く、または絶対パスで書く）を含めること
- `vault/rules/verifier/verifier.md` 自体は編集しない（`agent_write_guard.py` が拒否するため。実体への反映は人が手作業で行う）
- verifier の既存の判定ロジック（曖昧判定・緩さ判定・起点コミット検査等）に関する提案は不要。今回のスコープは書き込み先の明記のみ

## 進捗
