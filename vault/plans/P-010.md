---
id: P-010
status: approved
---
# ゴール
planner エージェント定義（`.claude/agents/planner.md`）に、ロードマップ「⑤ planner にルールを渡す」（`docs/roadmap-rules-extension.md`）の内容を反映する。planner が `bash scripts/rules.sh planner` の出力（`common/` と `planner/` のルール）を読み、タスク分割・受け入れ基準の下書きに反映する手順を追加する。あわせて `docs/vault-spec.md` の「9. 粒度の基準」に1〜2行追記する。run-queue・verifier・フック・install.sh・README.md は変更しない。

## 分割方針
成果物は `.claude/agents/planner.md` の1ファイル（付随して `docs/vault-spec.md` の該当節への1〜2行追記）で、編集箇所は「読むもの」「手順」「禁止」の3ブロックと `vault-spec.md` の1節に限定される。ロードマップ①〜④（P-006〜P-009）で確立済みの `scripts/rules.sh` を使うだけであり、新規のコード実装は無い。1コンテキストで終わる規模のため分割せず単一タスクとする。

## タスク一覧（順序・依存・各タスクの受け入れ基準の要約）
| 順序 | id | title | after | 受け入れ基準の要約 |
|---|---|---|---|---|
| 1 | T-0010 | planner.md にルール読み込み手順を追加し vault-spec.md の粒度基準を更新する | - | 「読むもの」に rules.sh planner の行、「手順」にルール反映と行数上限順守の記述、「禁止」に非禁止の明確化の一文、vault-spec.md 9節に vault/rules 言及を追加。smoke.sh が fail=0 |

## 計画の受け入れ基準
- 各タスクに成果物と受け入れ基準が1つずつある
- 依存に循環がない
- 1タスクが1コンテキストで終わる粒度である
