# T-03 plan/SKILL.md 手順A.4の planner 呼び出しに run_in_background: false を明記する

## 目的
`/plan` スキルの手順A.4（Agent ツールで `planner` サブエージェントを呼ぶ箇所）にも `run_in_background` の指定が無い。手順A.5〜7は planner の生成物を読んで人に提示することを前提にしており、`/run` の creator・verifier 呼び出しと同じ「呼び出し結果に後続手順が依存する」ケースに該当する。T-01 と同じ観点で `run_in_background: false` を明記する。

## 入力
- `.claude/skills/plan/SKILL.md` の「## A. 計画を作る（引数がゴールの時）」節、4番目の項目（planner を呼ぶ箇所。現状15行目）
- `vault/tasks/P-20260926-run-creator-verifier-sync/T-01.md`（run/SKILL.md での同種修正の記述ぶりを揃えるための参考。ただし本タスクは `.claude/skills/plan/SKILL.md` のみを変更し、`.claude/skills/run/SKILL.md` には触れない）

## 成果物
- `.claude/skills/plan/SKILL.md`（「## A. 計画を作る」節の手順4の1箇所のみを変更）

## 受け入れ基準
- 「## A. 計画を作る」節の手順4（planner 呼び出し）に `run_in_background: false` が明記されている。確認：`sed -n '/^## A\. 計画を作る/,/^## B\. 承認する/p' .claude/skills/plan/SKILL.md > /tmp/P-20260926-run-creator-verifier-sync-T03-secA.txt` を実行後、`grep -c 'run_in_background: false' /tmp/P-20260926-run-creator-verifier-sync-T03-secA.txt` が1以上
- 同箇所に、同期的に待つ理由（後続手順が結果に依存すること）の説明がある。確認：`grep -c -E '結果に依存|同期的' /tmp/P-20260926-run-creator-verifier-sync-T03-secA.txt` が1以上
- 変更ファイルが `.claude/skills/plan/SKILL.md` とこのタスク票自身以外に無い。確認：`git status --porcelain > /tmp/P-20260926-run-creator-verifier-sync-T03-status.txt` を実行後、中身が `.claude/skills/plan/SKILL.md` と `vault/tasks/P-20260926-run-creator-verifier-sync/T-03.md` の2行のみ

## 決定済み
- 追加方法は、既存の項目4（「Agent ツールで planner サブエージェントを呼ぶ。prompt にはゴールと、手順1で決めた計画 ID、関係するファイルのパスがあればそれを渡す。...」）の文中・文末に一文を追記する形にする。新しい番号付き項目を追加しない
- 追記する文言の趣旨は T-01 と同じ：「この Agent ツール呼び出しは `run_in_background: false` を指定し、同期的に完了を待つこと（手順5以降で生成物を読んで判定するため）」。`run_in_background: false` という文字列と、結果への依存・同期を示す語（「結果に依存」または「同期的」のいずれか）の両方を含めること
- `.claude/skills/run/SKILL.md` は本タスクの対象外（T-01 で別途対応済み）。誤って `.claude/skills/run/SKILL.md` を変更しないこと
- 「## B. 承認する」節は Agent ツールを呼ばない（frontmatter の status を書き換えるだけ）ため、本タスクの対象外
- 確認コマンドの一時ファイルは `/tmp/P-20260926-run-creator-verifier-sync-T03-*.txt` の固定パスを使う

## 進捗
- 起点コミット: 4a2faf7
- 読んだルール: vault/rules/common/git.md, vault/rules/common/roles.md, vault/rules/creator/creator.md, vault/rules/creator/git-workflow.md
- `.claude/skills/plan/SKILL.md` の「## A. 計画を作る」節・項目4（planner 呼び出し）の文末に `run_in_background: false` を指定し同期的に完了を待つ旨（「結果に依存する」を含む）を追記した。新しい番号付き項目は追加していない
- 受け入れ基準の確認コマンドを実行し、いずれも通過を確認：
  - `sed -n '/^## A\. 計画を作る/,/^## B\. 承認する/p' .claude/skills/plan/SKILL.md > /tmp/P-20260926-run-creator-verifier-sync-T03-secA.txt` → `grep -c 'run_in_background: false' /tmp/...-secA.txt` = 1
  - 同ファイルで `grep -c -E '結果に依存|同期的' /tmp/...-secA.txt` = 1
  - `git status --porcelain > /tmp/P-20260926-run-creator-verifier-sync-T03-status.txt` の中身は `.claude/skills/plan/SKILL.md` と `vault/tasks/P-20260926-run-creator-verifier-sync/T-03.md` の2行のみ
- `.claude/skills/run/SKILL.md` には触れていない
- 完了。review 待ち
