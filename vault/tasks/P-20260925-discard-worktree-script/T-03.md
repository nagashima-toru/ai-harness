# T-03 .claude/skills/run/SKILL.md 手順6.3.2 を discard_worktree.sh 呼び出しに置き換える

## 目的
`run` スキル手順6.3.2（verifier が FAIL で attempt が上限未満の時）の直接記述 `git worktree remove <worktree のパス> --force` と `git branch -D <ブランチ名>` を、T-01 で作った `scripts/discard_worktree.sh <worktree のパス> <ブランチ名>` の呼び出しに置き換える。

## 入力
- T-01 の成果物 `scripts/discard_worktree.sh`（引数の順序・意味）
- `.claude/skills/run/SKILL.md`（59行目付近、「## 6. verdict に従う（逐次処理）」の `3. FAIL の場合` → `2. attempt < 上限...` の段落）
- `vault/designs/D-006.md`（決定事項：PASS 時の片付け（55行目付近、`git worktree remove <worktree のパス>`＋`git branch -d <ブランチ名>`）は変更しない）

## 成果物
- `.claude/skills/run/SKILL.md`

## 受け入れ基準
- `grep -n "git branch -D" .claude/skills/run/SKILL.md` がヒットしない
- `grep -n "bash scripts/discard_worktree.sh <worktree のパス> <ブランチ名>" .claude/skills/run/SKILL.md` がヒットする
- PASS 時の片付け（手順6.3.1-4）は変更されていない。確認：`grep -n "git branch -d <ブランチ名>" .claude/skills/run/SKILL.md` が1件ヒットする（`-d` 小文字のまま）
- 旧記述の削除が該当1箇所に限られる。確認：`git diff -- .claude/skills/run/SKILL.md | grep -c '^-.*git worktree remove <worktree のパス> --force'` が `1`

## 決定済み
- 置き換え対象は「## 6. verdict に従う（逐次処理）」内の `3. FAIL の場合` → `2. attempt < 上限（...）なら` の段落だけ。手順6.3.3（上限到達で blocked）・コンフリクト時と新規コミット無し時に「worktree を残す」方針は変更しない
- 置き換え後の文言は `bash scripts/discard_worktree.sh <worktree のパス> <ブランチ名>` の形をそのまま使う（`<worktree のパス>` と `<ブランチ名>` はこの手順内で既に使われているプレースホルダ表現をそのまま流用する）
- 文脈の説明文（「対象タスクの worktree は破棄して作り直す方針を採る」等）は残してよい。変えるのはコマンド列挙の部分のみ
- `docs/vault-spec.md`・`docs/runbook.md` に同種の記述は無いことを確認済み（この計画の分割方針参照）。見つかっても本タスクの成果物には含めず、別途報告する（成果物は `SKILL.md` の1ファイルに限定するため）
- T-01 が先に完了している前提（`scripts/discard_worktree.sh` の引数順序が確定していること）

## 進捗
