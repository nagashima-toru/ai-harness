# T-0013 README.md に「拡張ポイント（ルール）」節を追加し runbook にルール追加手順を書く

## 目的
`README.md` にルール拡張ポイント（`vault/rules/`）の使い方を説明する節を追加し、`docs/runbook.md` に「ルールを足す」手順を追加する。

## 入力
- `README.md`（現状。「## 使い方」「## 仕組み」「## 構成」の各節）
- `docs/runbook.md`（現状。番号付き手順 1〜5、「## 困ったとき」）
- `docs/vault-spec.md` の「12. ルール」「13. agent_write_guard フックの改ざん防止判定」節（説明の正本）
- `docs/roadmap-rules-extension.md` の「## ⑦ install.sh と README」節（T-b の受け入れ基準の候補・決定済み）
- T-0012（`scripts/install.sh` がルール拡張ポイントを複製すること。完了が前提）

## 成果物
- `README.md` の「## 拡張ポイント（ルール）」節（付随：同ファイルの「## 構成」ツリー更新、`docs/runbook.md` への追記）

## 受け入れ基準
- `README.md` に `## 拡張ポイント（ルール）` の見出しが1つあり、その節（次の `## ` 見出しの直前まで）が25行以内で、`common`・`creator`・`verifier`・`planner` の4語すべてを含む（確認：`awk '/^## 拡張ポイント/,/^## [^拡]/' README.md` で節を切り出し、行数と4語の有無を確認）
- 節に「受け入れ基準からルールを参照する書き方」の例が1つ含まれる（例：`vault/rules/common/xxx.md に従っている` のようにルールファイルを名指しする一文。確認：節の中に `vault/rules/` を含む例示行が1件以上ある）
- 節に「doing/review 中は `vault/rules/` を編集できない（フックで拒否される）」旨の記述がある（確認：節の中に `doing` と `review` と `編集` を含む行がある）
- `README.md` の「## 構成」ツリーに `vault/rules/` と `scripts/rules.sh` の記載が追加されている（確認：`grep -c 'vault/rules/' README.md` ≥ 2 かつ `grep -c 'rules.sh' README.md` ≥ 2、うち1件ずつが「## 構成」節内にある）
- `docs/runbook.md` に「ルールを足す」手順が追加されており、本文（見出し行を除く）が5行以内（確認：`grep -n 'ルールを足す' docs/runbook.md`）

## 決定済み
- 新節の位置は「## 使い方」と「## 仕組み」の間（ロードマップの決定どおり）
- 節に書く4点：(1) `vault/rules/common・creator・verifier・planner` の4ディレクトリと「全員／作成のみ／検証のみ／計画のみ」の対応、(2) インストール先で書く前提でありハーネス本体はルールを同梱しないこと（`vault/rules/` 配下は `.gitkeep` 以外の実ルールを持たない）、(3) 受け入れ基準からルールを参照する書き方の例を1つ、(4) doing/review 中はルールを編集できない（フックで拒否される）こと
- 25行の数え方は見出し行 `## 拡張ポイント（ルール）` を含めず、次の `## ` 見出しの直前までの本文行（空行含む）で数える
- `docs/runbook.md` の追記位置・見出し名は自由（例：既存の番号付き手順の後に「## 6. ルールを足す」を追加する等）。5行以内に収まる範囲で「ファイルを置く→どのディレクトリか決める→受け入れ基準から参照する」の3手順が読み取れればよい
- `scripts/smoke.sh`・`.claude/` 配下・`docs/vault-spec.md` はこのタスクでは変更しない

## 進捗
- `bash scripts/rules.sh creator` を実行（着手時のルール読み込み手順）。出力は空だったため何もしていない
- `README.md` に「## 拡張ポイント（ルール）」節を「## 使い方」と「## 仕組み」の間に追加（13行、4ディレクトリの対応表・参照例・doing/review中は編集不可の3点を含む）
- 「## 構成」ツリーの vault/ 行・scripts/ 行を更新し `vault/rules/`・`rules.sh` を反映
- `docs/runbook.md` に「## 6. ルールを足す」を「## 困ったとき」の前に追加（本文4行）
- 受け入れ基準5件の確認コマンドをすべて実行し通過。README全体で vault/rules/=7件・rules.sh=2件、構成節内にそれぞれ1件以上。`bash scripts/smoke.sh` は変わらず pass=40 fail=0
