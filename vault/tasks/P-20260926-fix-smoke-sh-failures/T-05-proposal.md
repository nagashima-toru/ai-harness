# 提案: vault/rules/planner/planner.md への追記案

## 追記先
`vault/rules/planner/planner.md` の「やらないこと（このリポジトリで実際に起きた失敗から）」節。
既存の箇条書き項目群（T-0002・T-0016・T-0012/T-0013 等の項目）の最後に、見出し無しの箇条書きとして次の1項目を追加する。

## 追記する文章（下書き）

- `bash scripts/smoke.sh` のような、対象が広く環境依存になりうる網羅テストの「全件PASS」をそのまま確認コマンドにしない。
  着手時（doing 開始時）に着手前 HEAD で一度実行し、結果（`pass=<N> fail=<M>` と失敗ケース名）をタスク票の「進捗」に記録させ、受け入れ基準は「新規追加分がすべて `ok`」「既存の失敗件数・内容が着手前から悪化していない」の2点にする。
  `P-20260926-write-guard-worktree-delegate` の T-01 では「全件PASSし rc=0」をそのまま基準にしたため、今回の変更と無関係な既存の環境依存の失敗（macOS 固有のシンボリックリンク解決の食い違いや、未削除 worktree に由来するフレーキーな失敗）のせいで blocked にせざるを得なくなった（issue #59）。後続の `P-20260926-hook-worktree-delegate-phase2` の T-01 では、この運用をあらかじめ「決定済み」に書いたことで同じ blocked を避けられている。

## この追記案の確認方法（このタスクの受け入れ基準に対応）
- 追記先の見出し名を明示：「やらないこと（このリポジトリで実際に起きた失敗から）」節
- 「着手前」の語を含む一文で、着手前 HEAD の実行結果をベースラインとして記録する運用を明記
- 根拠として issue #59 と `P-20260926-write-guard-worktree-delegate` の blocked 実例を明記

## 反映方法
実体（`vault/rules/planner/planner.md`）への反映は人が手作業で行う。このタスク（T-05）はこの下書きの作成までで、実体の書き換えは行わない。
