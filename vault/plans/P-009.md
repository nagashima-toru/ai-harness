---
id: P-009
status: done
---
# ゴール
verifier エージェント定義（`.claude/agents/verifier.md`）にルール（`vault/rules/`）の読み込みを追加する。受け入れ基準がルールを参照している行は、該当ルールファイルを根拠に真偽を判定し、note に参照したルールファイルのパスを書く。ルールを参照していない基準をルールで落とすことは禁止する。あわせて `docs/vault-spec.md` の verdict.json 節に note の書き方を1〜2行追記する。

これは `docs/roadmap-rules-extension.md` の「## ④ verifier にルールを渡す」に対応する。①〜③（P-006/T-0006、P-007/T-0007、P-008/T-0008）は完了済みで、`vault/rules/{common,creator,verifier,planner}/`・`docs/vault-spec.md` の「12. ルール」節・`scripts/rules.sh`・run-queue のルール読み込みは既に存在する。

## 分割方針
成果物は `.claude/agents/verifier.md`（主）と `docs/vault-spec.md`（付随の1〜2行）の2ファイルのみで、変更内容も密結合（同じ「note にルールファイルのパスを書く」という1つの仕様を、verifier の指示書と正本の両方に反映するだけ）なので、分割せず1タスクとする。run-queue・planner・フック・install.sh・README.md は対象外（このプランでは変更しない）。

## タスク一覧（順序・依存・各タスクの受け入れ基準の要約）
| 順序 | id | title | after | 受け入れ基準の要約 |
|---|---|---|---|---|
| 1 | T-0009 | verifier にルール読み込みと参照時の判定手順を追加する | - | verifier.md の「読むもの」に rules.sh verifier の行、「手順」にルール参照時の判定・note 記述、「禁止」に無関係な基準をルールで落とさない旨を追加。vault-spec.md の verdict.json 節に note の書き方を追記。他ファイル無変更。smoke.sh が fail=0 |

## 計画の受け入れ基準
- 各タスクに成果物と受け入れ基準が1つずつある
- 依存に循環がない
- 1タスクが1コンテキストで終わる粒度である
</content>
