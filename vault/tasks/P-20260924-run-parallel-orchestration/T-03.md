# T-03 マージ・状態遷移（done/blocked）・attempt 更新の逐次処理と回帰確認

## 目的
`run` スキルに、全 verifier 完了後の逐次後処理（PASS 時のマージ・`review→done`・ログ追記・worktree 削除、コンフリクト時の `blocked` 化、FAIL 時の attempt 更新）を追加し、着手可能集合が1件の場合でもフェーズ2までの逐次フローと同じ結果になることを確認する手順を明記する。

## 入力
- `vault/designs/D-003.md`（フェーズ3節。決定済み：「マージは常にオーケストレーターが計画ブランチ上で逐次実行」「コンフリクト時は自己判断で解決せず blocked」）
- `.claude/skills/run/SKILL.md`（T-02 完了後の版。手順6「verdict に従う」・手順7「次へ・完了」）
- `vault/rules/common/git.md`（コンフリクト時に自己判断で解決しない方針）
- `vault/plans/P-20260924-creator-subagent.md` と配下タスク票（フェーズ2までの逐次フローの実例。回帰確認の比較対象）

## 成果物
- `.claude/skills/run/SKILL.md` の手順6「verdict に従う」・手順7「次へ・完了」の書き換え、および回帰確認手順の追記

## 受け入れ基準
- 全 verifier 完了後、タスクを1件ずつ逐次処理する旨が明記されている。確認: `grep -n "逐次" .claude/skills/run/SKILL.md`
- PASS 時に該当 worktree のブランチを `git merge --no-ff` で計画ブランチへ取り込み、成功したら `review→done` にしてログ追記し worktree を削除する手順が明記されている。確認: `grep -n -- "--no-ff" .claude/skills/run/SKILL.md`
- マージでコンフリクトが起きた場合にタスクを `blocked` にし question にコンフリクトの状況を書き、worktree を削除せず残す手順が明記されている。確認: `grep -n "コンフリクト" .claude/skills/run/SKILL.md`
- FAIL の場合は計画ブランチへマージせず、attempt が上限未満なら `doing`（attempt+1）、上限到達なら `blocked` にし、いずれもログに追記する手順が明記されている
- 着手可能集合が1件だけの場合に、計画票・ログ・成果物の最終状態が `P-20260924-creator-subagent` で実施したフェーズ2までの逐次フローと同じ結果になることを確認する手順が明記されている
- `HARNESS_MAX_ATTEMPTS` による attempt 上限判定のロジック自体（既定値・比較条件）が変更されていない。確認: `grep -n "HARNESS_MAX_ATTEMPTS" .claude/skills/run/SKILL.md` の既存記述（既定 3）がそのまま残っていることを確認する

## 決定済み
- worktree を次 attempt で再利用するか、破棄して作り直すかは実装時に決めてよい。選んだ方針を SKILL.md に明記すること
- 複数タスクが PASS した場合のマージ順序は、計画票のタスク表の並び順（上から）で1件ずつ処理する
- FAIL で attempt 上限に達し `blocked` になったタスクの worktree は削除しなくてよい（人が blocked を解消する調査に使える可能性があるため、コンフリクト時の温存方針と揃える）
- コンフリクト解消は自己判断で行わない（`vault/rules/common/git.md` の既存方針を踏襲）。他の並行タスクの処理は止めない
- 回帰確認は「手順を文書として書く」ことが成果物であり、このタスクの中で実際に1タスクの計画を用意して `/run` を通しで実行することまでは必須にしない（実施できるならしてよいが、必須の確認コマンドにはしない）

## 進捗
- 読んだルール: git.md, roles.md, creator.md, git-workflow.md
- `vault/designs/D-003.md` フェーズ3節、`.claude/skills/run/SKILL.md`（T-02 完了後の版）、`vault/rules/common/git.md`、`vault/plans/P-20260924-creator-subagent.md` と配下タスク票（T-04 の進捗＝フェーズ2逐次フローの実例）を確認した
- `.claude/skills/run/SKILL.md` の手順6「verdict に従う」を「verdict に従う（逐次処理）」に書き換え：
  - 全 verifier 完了後、対象タスクを計画票のタスク表の上から並んだ順に1件ずつ逐次処理する旨を明記
  - PASS：`git merge --no-ff <worktree のブランチ名>` で計画ブランチへ取り込み、成功したら `review→done` + ログ追記 + `git worktree remove` / `git branch -d` で worktree・ブランチを削除する手順を明記
  - マージコンフリクト時：自己判断で解決せず `git merge --abort` の上で `blocked` にし question にコンフリクト状況を書き、ログに `review→blocked ... マージコンフリクト` を追記、worktree は削除せず残す。他タスクの処理は止めない旨を明記
  - FAIL：計画ブランチへマージせず、attempt<上限（`HARNESS_MAX_ATTEMPTS`、既定3）なら `doing`(attempt+1)+ログ追記、上限到達なら `blocked`+ログ追記。既存の「手順3の1から繰り返す」参照はそのまま維持
  - 決定済みに基づき worktree の扱いを明記：PASS 後は削除、FAIL 再試行時（上限未到達）は破棄して次回 creator 呼び出し時に作り直す方針を採用（理由：マージしていない不採用の変更のため）、FAIL 上限到達時とコンフリクト時は削除せず温存（人の調査用、決定済みの方針を踏襲）
- 手順7「次へ・完了」は変更なし（既存のまま）。新規節「## 回帰確認（着手可能集合が1件だけの場合）」を手順7の後に追加し、`P-20260924-creator-subagent` のフェーズ2逐次フローと最終結果が同じになることを確認する観点（計画票のタスク表の最終状態／ログの遷移行の書式と行数／成果物の差分／verdict の attempt 一致／worktree・作業ブランチの後始末）を5点の確認手順として明記。決定済みの通り「手順を文書として書く」ことを成果物とし、実際に計画を用意して `/run` を通しで実行することは必須にしていない
- 確認コマンド実行結果：
  - `grep -n "逐次" .claude/skills/run/SKILL.md` → 28・46・47・67・74行目にヒット
  - `grep -n -- "--no-ff" .claude/skills/run/SKILL.md` → 52行目にヒット
  - `grep -n "コンフリクト" .claude/skills/run/SKILL.md` → 54・58・68・72行目にヒット
  - `grep -n "HARNESS_MAX_ATTEMPTS" .claude/skills/run/SKILL.md` → 21・57行目にヒット。57行目の「既定 3」の記述・比較条件（attempt<上限→doing+1、attempt≥上限→blocked）は変更前と同一のロジックを維持
  - SKILL.md 全体を Read で通し読みし、手順1〜7＋回帰確認節が矛盾なく繋がっていることを確認
- `git status --porcelain` で `.claude/skills/run/SKILL.md` 以外に自分が新規に書き込んだファイルが無いことを確認（`docs/vault-spec.md`・`vault/plans/...`・`vault/tasks/.../T-01.md`・`T-02.md` の差分は T-01／T-02 由来の既存差分で、今回自分が作ったものではない）
</content>
