# T-03 .claude/ai-harness.md を新方式に書き直す

## 目的
CLAUDE.md が `@` で読み込む本文 `.claude/ai-harness.md` を、計画票がキューになる新方式（1セッション
=1計画=1ブランチ、`/plan`・`/run`）で書き直す。`vault/todo.md` への言及をすべて消す。

## 入力
- `.claude/ai-harness.md`（現行。書き換え対象）
- `docs/vault-spec.md`（正本。特に「1. ディレクトリ」「2. 状態」節）
- `.claude/skills/plan/SKILL.md`・`.claude/skills/run/SKILL.md`（新方式で既に書かれている。用語を合わせる）
- `vault/rules/common/roles.md`

## 成果物
- `.claude/ai-harness.md`

## 受け入れ基準
- `vault/todo.md` への言及が消えている。確認：`grep -c 'todo.md' .claude/ai-harness.md` が0
- 「状態遷移」に `doing` が計画内で常に1件である旨が書かれている。確認：`grep -c '計画内で常に1件' .claude/ai-harness.md` が1以上
- 冒頭またはいずれかの節に「1セッション=1計画=1ブランチ」の運用モデルが明記されている。確認：`grep -c '1セッション.*1計画.*1ブランチ' .claude/ai-harness.md` が1以上
- 「スキル」節が `/run-queue` ではなく `/run` を挙げている。確認：`grep -c '/run-queue' .claude/ai-harness.md` が0 かつ `grep -c '/run' .claude/ai-harness.md` が1以上
- 「禁止」に計画票のタスク表の列順・見出し名を変えない旨が明記されている（`todo.md` の列順という表現は使わない）。確認：`grep -c 'タスク表の列順・見出し名' .claude/ai-harness.md` が1以上
- 見出し構成が変わらず6個のまま（`## 開始時に読む順`・`## 状態遷移（5つで固定）`・`## 質問のしかた`・`## 役割`・`## スキル`・`## 禁止`）。確認：`grep -c '^## ' .claude/ai-harness.md` が6

## 決定済み
- 新しい本文の下書き（そのまま採用してよい。文言の微調整は可）：
  ```
  作業は「作成 → 検証」の二段構成。検証（verifier）が PASS しない限り完了できない。状態はすべて
  `vault/` のファイルにあり、仕様の正本は `docs/vault-spec.md`。1セッション=1計画=1ブランチで、計画
  の作成から実行までを1本のブランチに閉じる。

  ## 開始時に読む順
  1. `vault/plans/*.md` を走査し、frontmatter の `status` が `approved` の計画票を1つ見つける
  2. その計画票の「タスク表」を読む。`doing` の行があれば `vault/tasks/<計画ID>/<id>.md` の「進捗」から再開する
  3. 無ければ `todo` の先頭（`after` が全部 `done` のもの）を取る
  4. 「次は何をやる？」と聞かれたら、承認済み計画票のタスク表を読んで先頭タスクの ID と title を答える

  ## 状態遷移（5つで固定）
  `todo → doing → review → (done | doing[attempt+1] | blocked)`。`blocked → todo` は人だけ。
  - `doing` は計画内で常に1件だけ。2件目を取らない
  - `done` にできるのは `vault/verdicts/<計画ID>/<id>.json` が PASS で、`attempt` が計画票のタスク表と一致する時だけ
  - 状態を変えたら `vault/log/<計画ID>.md` に1行追記する（`- YYYY-MM-DD HH:MM T-01 doing→review attempt=1 補足`）
  - 日時は JST：`TZ=Asia/Tokyo date '+%Y-%m-%d %H:%M'`

  ## 質問のしかた
  - 着手前にまとめて出す。着手後は質問せず、判断が必要になったら `blocked` にして question 列に書く
  - タスク票の「決定済み」にある回答を優先する。勝手に決めない、勝手に広げない

  ## 役割
  - 作成エージェント（メイン）：タスクを取り、着手時に `bash scripts/rules.sh creator` の列挙を読み、成果物を作り、状態と log を更新する
  - `verifier` は `vault/verdicts/`、`planner` は `vault/plans/`・`vault/tasks/` にだけ書く（詳細は `vault/rules/common/roles.md`）

  ## スキル
  `/plan <ゴール>`（計画を draft で作りブランチを切る。`/plan approve <計画ID>` で承認）・`/run`（自分のブランチの承認済み計画を1タスク処理する）・`/design <ゴール>`（大きなゴールを設計文書にする）

  ## 禁止
  - `done` のタスク票・verdict を編集すること
  - `rm -rf`、`git push --force` などの破壊的操作（settings.json で拒否）
  - 計画票のタスク表の列順・見出し名を変えること（フックが表を解析する）
  ```
- git worktree・ブランチ運用の詳細ルール（コミット粒度・main 直コミット拒否など）はフェーズ5で
  `vault/rules/` に追加する。このタスクでは「1セッション=1計画=1ブランチ」の一文以上の詳細を書かない
- `CLAUDE.md` 自体は変更しない（`@.claude/ai-harness.md` の1行を読み込むだけの薄いファイルのため）

## 進捗
- 読んだルール: roles.md, creator.md（既読）
- `.claude/ai-harness.md` を決定済みの下書きどおりに全面書き換え（見出し6つは維持）
- 受け入れ基準の確認コマンドを実行し、6件すべて通した
  - `grep -c 'todo.md'`=0 / `計画内で常に1件`=1 / `1セッション.*1計画.*1ブランチ`=1
  - `/run-queue`=0 / `/run`=1 / `タスク表の列順・見出し名`=1
  - `grep -c '^## '`=6
- コードは触っていないが念のため `bash scripts/smoke.sh | tail -1` を実行 → `smoke: pass=95 fail=0`
