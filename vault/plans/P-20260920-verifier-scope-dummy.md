---
id: P-20260920-verifier-scope-dummy
status: draft
---
# ゴール
T-05 の実地テスト用のダミー計画（2回目）。verifier の新責務2つ（基準の妥当性判定・宣言外ファイルの
変更検査）が実際に働くかを確かめるためだけに存在する。検証後に削除する。

`status` は `draft` のままにする（`approved` にすると本計画と合わせて2件になり `plan_guard.py` が
異常として検出する）。

## 分割方針
実地テスト専用のため1タスクのみ。

## タスク表（状態の正本）
| id | status | attempt | after | title | question |
|---|---|---|---|---|---|
| T-01 | review | 1 | - | ダミー：README に語が含まれることを確かめる | |

## 計画の受け入れ基準
- 実地テスト用のダミーであり、本番の運用には使わない
